if [ -f "$HOME/.profile" ]; then
    source $HOME/.profile
fi

# Where should I put you?
bindkey -s ^ff "tmux-sessionizer\n"
bindkey '^P' history-substring-search-up
bindkey '^N' history-substring-search-down
