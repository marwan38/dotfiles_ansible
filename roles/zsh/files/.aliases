alias ta="tmux attach"
alias tK="tmux kill-server 2> /dev/null"
alias cb='git branch --sort=-committerdate | fzf --header Checkout| xargs git checkout'
alias n="nvim"

# Project based
# TODO: Move to a 'personal' alias file that's hidden from the public repo
# AP
alias rs="./devops/rsync.sh marwan"
alias sbuild="./vendor/bin/sake dev/build"
